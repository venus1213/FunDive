'use client';

import * as React from 'react';
import { ShareButtons, ShareButtonsProps } from '@/components/ui/share-buttons';

export default function ShareButtonsWrapper(props: ShareButtonsProps) {
  return <ShareButtons {...props} />;
} 